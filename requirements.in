click              # Helper for command-line tool niceties
inquirer           # Just needed for IO test
mariadb            # Persistence via mySQL-like DB
RPi.GPIO==0.7.1a4  # Potentially more specific than needed, but matching IO in system setup
